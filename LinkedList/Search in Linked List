class Solution {
    public boolean searchKey(ListNode head, int key) {
        // Your code goes here
        while(head!=null){
            if(head.val==key){
                return true;
            }
            head=head.next;
        }
        return false;
    }
}
